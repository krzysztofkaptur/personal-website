---
import Layout from '../layouts/Layout.astro';

const posts = await Astro.glob('../pages/blog/*.md')

---

<Layout title="Blog | Kris.dev">
	<main>
		<h1 class="text-3xl">Blog</h1>

    {posts.map(post => <li><a href={post.url}>{post.frontmatter.title}</a></li>)}
	</main>
</Layout>
